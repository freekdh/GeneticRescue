CC = gcc
CFLAGS = -Wall -g -O2 -I/usr/local/lib/R/site-library/Rcpp/include/-I /usr/share/R/include/
DEPS = random.h utils.h
OBJ = BDSimulations.o random.o utils.o

%.o: %.c $(DEPS)
	$(CC) $(CFLAGS) -c -o $@ $<

go: $(OBJ)
	gcc $(CFLAGS) -o $@ $^
